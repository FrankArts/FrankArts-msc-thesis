Scripts used to actually train the RL models.

robogym_wrapper trains the RL model that uses task_ids (one-hot encoded vectors) as task input.
robogym_embeddings_wrapper trains the RL model that uses embeddings as task input.